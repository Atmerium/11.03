<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
    <title>Zsákba Macska Mozi Foglalás</title>
</head>
<body class="bg-light">
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <h1 class="mb-4">Foglalás</h1>

                <% if (errors && errors.length > 0) { %>
                    <div class="mb-3">
                        <div class="alert alert-danger" role="alert">
                            <ul class="mb-0">
                                <% errors.forEach(function(error) { %>
                                    <li><%= error %></li>
                                <% }); %>
                            </ul>
                        </div>
                    </div>
                <% } %>

                <form method="post" action="/booking" class="card p-4 bg-white">
                    <div class="mb-3">
                        <label for="name" class="form-label">Név <span class="text-danger">*</span></label>
                        <input id="name" name="name" type="text" class="form-control" value="<%= values.name %>">
                    </div>

                    <div class="mb-3">
                        <label for="email" class="form-label">E-mail <span class="text-danger">*</span></label>
                        <input id="email" name="email" type="email" class="form-control" value="<%= values.email %>">
                        <div class="form-text">Legalább 1-1 karakter a @ előtt és után (pl. abc@def.hu)</div>
                    </div>

                    <div class="mb-3">
                        <label for="datetime" class="form-label">Dátum és időpont <span class="text-danger">*</span></label>
                        <input id="datetime" name="datetime" type="datetime-local" class="form-control" value="<%= values.datetime %>">
                    </div>

                    <div class="mb-3">
                        <label for="guests" class="form-label">Nézők száma <span class="text-danger">*</span></label>
                        <input id="guests" name="guests" type="number" class="form-control" min="1" max="10" value="<%= values.guests %>">
                        <div class="form-text">Minimum 1, maximum 10 fő</div>
                    </div>

                    <div class="d-flex justify-content-end">
                        <button type="submit" class="btn btn-primary">Foglalás</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
